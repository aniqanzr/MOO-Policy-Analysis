"""The problem definition: quota formula, objectives, constraints.

The quota is computed by the LTA Annex A formula, never chosen. The decision variables are
the policy parameters that feed it: g_ab, g_c and theta. Constraints are value judgements
and stay separate from dominance, in the data structures and downstream in the UI.
"""
