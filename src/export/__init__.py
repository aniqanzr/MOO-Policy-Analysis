"""JSON artifact writers.

Everything the static frontend reads is written here: the frontier, the feasible cloud with
dominated and constraint-excluded points distinguished, the weight simplex map, and the
sensitivity bands.
"""
